<nav class="navbar navbar-top navbar-expand-md navbar-dark" id="navbar-main">
  <div class="container-fluid">
    <!-- Brand -->
    <a class="h4 mb-0 text-white text-uppercase d-none d-lg-inline-block" href="#"><i class="<%= icon_class %>"></i> <%= page_name %></a>
    <!-- Form -->

    <!-- User -->
    <ul class="navbar-nav align-items-center d-none d-md-flex">
      <li class="nav-item dropdown">
        <a class="nav-link pr-0" href="#" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <div class="media align-items-center">
            <div class="media-body ml-2 d-none d-lg-block">
              <span class="avatar avatar-sm rounded-circle"><i class="zmdi zmdi-account"></i></span>
              <span class="mb-0 text-sm font-weight-bold"><%= current_user.full_name %></span>
            </div>
          </div>
        </a>
        <div class="dropdown-menu dropdown-menu-arrow dropdown-menu-right">
          <div class=" dropdown-header noti-title">
            <h6 class="text-overflow m-0">Welcome!</h6>
          </div>
          <%= link_to shops_path, class: "dropdown-item" do %>
            <i class="fa fa-cogs"></i>
            <span>Settings</span>
          <% end %>
          <%= link_to destroy_user_session_path, :method => :delete, class: "dropdown-item" do %>
            <i class="ni ni-user-run"></i>
            <span>Logout</span>
          <% end %>
        </div>
      </li>
    </ul>
  </div>
</nav>
<% if params[:controller].downcase == "dashbords" %>
  <div class="header bg-gradient-primary pb-6">
    <div class="container-fluid">
      <div class="header-body">
      </div>
    </div>
  </div>
<% else %>
  <div class="header bg-gradient-primary pb-7 pt-4">
    <div class="container-fluid">
      <div class="header-body">
      </div>
    </div>
  </div>
<% end %>